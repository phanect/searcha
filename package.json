{
  "name": "@phanect/datasheet-monorepo",
  "version": "0.0.0",
  "description": "Convert your dataset to websites or internal wikis",
  "author": {
    "name": "Jumpei Ogawa",
    "url": "https://phanective.org"
  },
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "git clean -dX --exclude=\"!.env\" --force",
    "refresh": "pnpm clean && rm --force ./pnpm-lock.yaml && pnpm install && pnpm build"
  },
  "devDependencies": {
    "@types/node": "^24.12.0"
  },
  "engines": {
    "node": "24.x"
  }
}
